// main.js
// 
// date = current date
// input = number input
// day = "days" selected
// week = "weeks" selected
// remind = final reminder date

function setDate (date, input) {
	var date = document.getElementById('currentdate');
	var input = document.getElementById('userinput');
	var day = document.getElementById('selectday');
	var week = document.getElementById('selectweek');
	var remind = document.getElementById('reminderdate');

	for (every time user selects date range) {
		if (user selects between day / week) {
			return true;
		else {
			return false;
		}
		}
	}
}
